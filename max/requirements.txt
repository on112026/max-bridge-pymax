# Зависимости max-процесса (PyMax-клиент + sender/chat_ops циклы).
#
# Этот файл — самодостаточный: max/ можно собрать отдельно
# (`pip install -r requirements.txt -r max/requirements.txt`).
# Общие с api/bot зависимости (SQLAlchemy, pydantic, httpx) дублируются
# здесь намеренно — pip всё равно дедуплицирует.

# --- PyMax runtime deps (из PyMax/pyproject.toml, version 2.2.0) ---
aiofiles>=23.2.1,<24.2
aiohttp==3.10.10
aiosqlite>=0.22.1
msgpack>=1.1.2
pydantic>=2.4.1,<2.10
python-socks[asyncio]>=2.8.1
qrcode>=8.2
websockets>=16.0

# --- Common (общие с api/bot, см. комментарий выше) ---
httpx==0.28.1
SQLAlchemy==2.0.36