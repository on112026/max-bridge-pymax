# Общие runtime-зависимости max-bridge-pymax.
#
# PyMax вендорится в ./vendor/pymax и НЕ ставится из PyPI (см. vendor/README.md).
# Его runtime-зависимости вынесены в max/requirements.txt, чтобы пакет
# max/ был самодостаточным (собрать max-процесс отдельно = взять
# requirements.txt + max/requirements.txt).

# --- HTTP API (FastAPI) ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- БД и схемы ---
SQLAlchemy==2.0.36
pydantic>=2.4.1,<2.10

# --- HTTP-клиент (бот↔API, max↔API) ---
httpx==0.28.1

# --- Multipart для /sessions/upload (UploadFile в FastAPI) ---
python-multipart==0.0.9